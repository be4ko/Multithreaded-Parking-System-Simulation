import java.util.ArrayList;

public class Main {
    // public static void main(String[] args) {
    // ParkingSpots parkingSpots = new ParkingSpots();
    // Semaphore semaphore = new Semaphore(4);

    // ArrayList<Car> gate1Cars = new ArrayList<>();
    // gate1Cars.add(new Car(0, 0, 3, new Gate(1, gate1Cars), parkingSpots,
    // semaphore));
    // gate1Cars.add(new Car(1, 0, 4, new Gate(1, gate1Cars), parkingSpots,
    // semaphore));
    // gate1Cars.add(new Car(2, 0, 2, new Gate(1, gate1Cars), parkingSpots,
    // semaphore));
    // gate1Cars.add(new Car(3, 0, 5, new Gate(1, gate1Cars), parkingSpots,
    // semaphore));

    // ArrayList<Car> gate2Cars = new ArrayList<>();
    // gate2Cars.add(new Car(4, 1, 4, new Gate(2, gate2Cars), parkingSpots,
    // semaphore));
    // gate2Cars.add(new Car(5, 1, 3, new Gate(2, gate2Cars), parkingSpots,
    // semaphore));

    // ArrayList<Car> gate3Cars = new ArrayList<>();
    // gate3Cars.add(new Car(6, 2, 3, new Gate(3, gate3Cars), parkingSpots,
    // semaphore));
    // gate3Cars.add(new Car(7, 2, 4, new Gate(3, gate3Cars), parkingSpots,
    // semaphore));

    // for (Car car : gate1Cars)
    // new Thread(car).start();
    // for (Car car : gate2Cars)
    // new Thread(car).start();
    // for (Car car : gate3Cars)
    // new Thread(car).start();
    // }
}